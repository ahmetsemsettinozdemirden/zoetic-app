import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollView: {
    padding: 24,
  },
  button: {
    padding: 24,
  }
});

export default styles;
